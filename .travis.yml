language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t geonil/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true geonil/react-test-app npm run test

after_success:
  - docker build -t geonil/docker-frontend ./frontend
  - docker build -t geonil/docker-backend ./backend
  - docker build -t geonil/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push geonil/docker-frontend
  - docker push geonil/docker-backend
  - docker push geonil/docker-nginx
